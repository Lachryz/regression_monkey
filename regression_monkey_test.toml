# regression_monkey_test.toml
# 测试配置——配合合成 CSV，不依赖真实 .dta 数据文件。
# data 字段在测试中由代码动态注入正确路径，此处为占位符。

data = "test_data.csv"

y = ["outcome"]
x = ["treatment"]
controls_test = ["ctrl_a", "ctrl_b"]
controls_must = ["ctrl_c"]

output = "test_outputs"
dpi = 72
fig_width = 8.0
n_jobs = 1

Firm_FE   = "firm_id"
Ind_FE    = "industry"
Time_FE   = "year"

absorb_firm_time_vce_cluster_firm    = true
absorb_firm_indtime_vce_cluster_firm = false
